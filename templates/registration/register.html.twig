{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}



{% block body %}

<div class="container">
<div class="row">
<div class="col-md-8 mx-auto">
<h1 class="h3 mb-3 font-weight-normal"> Register</h1>
 {{ form_start(registrationForm) }}
<table class="table">
  
    <tbody>
     <tr>
     <td></td>
     <td></td>
     <td></td>
     <td></td>
      </tr>
        <tr>
            <td>{{ form_row(registrationForm.email) }}</td>
            <td>{{ form_row(registrationForm.plainPassword, {
    label: 'Password'
}) }}</td>
            <td>{{ form_row(registrationForm.agreeTerms) }}</td>
            <td>
             <button class="btn btn-lg btn-success btn-lg btn-block mt-2" type="submit">
Register    </button>
            </td>
        </tr>
        
    </tbody>
</table>

 
{{ form_end(registrationForm) }}
</div>
</div>
</div>
    
{% endblock %}
